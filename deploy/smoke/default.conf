server {
    listen 8080;
    default_type application/json;
    location = /readyz {
        return 200 '{"status":"ok","mode":"infrastructure-smoke"}\n';
    }
    location / {
        return 200 '{"mode":"infrastructure-smoke","message":"Compose and tunnel origin are reachable; the application is not deployed."}\n';
    }
}
