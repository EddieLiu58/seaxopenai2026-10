DATABASE_URL=jdbc:postgresql://localhost:5432/seax
DATABASE_USERNAME=seax
DATABASE_PASSWORD=seax
DATABASE_PORT=5432
# Optional overrides; the POC defaults are in application.properties.
# OPENAI_API_KEY=your-key
# OPENAI_MODEL=gpt-5.5
PORT=8080
CORS_ALLOWED_ORIGINS=http://localhost:3000
# Only seeds the supplied software-company organization when no memory exists.
SEED_GLOBAL_MEMORY=true
WORKER_ENABLED=true
