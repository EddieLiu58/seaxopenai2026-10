# Include /api/v1. Unset uses the same-origin /api/v1 endpoint.
# Rebuild static exports after changing this public URL.
NEXT_PUBLIC_API_BASE_URL=https://solve-batteries-discs-lens.trycloudflare.com/api/v1
